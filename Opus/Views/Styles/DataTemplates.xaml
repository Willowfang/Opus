<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dialog="clr-namespace:Opus.Services.Implementation.UI.Dialogs;assembly=Opus.Services.Implementation"
                    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:res="clr-namespace:Opus.Resources">

    <!--MESSAGE DIALOG-->
    <DataTemplate DataType="{x:Type dialog:MessageDialog}">
        <StackPanel>
            <TextBlock HorizontalAlignment="Center"
                       Style="{DynamicResource MaterialDesignCaptionTextBlock}"
                       Margin="32, 32, 32, 10"
                       Text="{Binding Content}"
                       MinWidth="150" MaxWidth="250"
                       TextWrapping="Wrap" TextAlignment="Center"
                       FontSize="14"/>
            <Button HorizontalAlignment="Center"
                    Margin="16"
                    Command="{Binding Close}"
                    Style="{DynamicResource MaterialDesignOutlinedButton}"
                    Content="OK"/>
        </StackPanel>
    </DataTemplate>

    <!--COMPOSITION PROFILE DIALOG-->
    <DataTemplate DataType="{x:Type dialog:CompositionProfileDialog}">
        <StackPanel>
            <TextBox Style="{DynamicResource MaterialDesignFloatingHintTextBox}"
                     md:HintAssist.Hint="{x:Static res:Labels.CompositionProfileDialog_Name}"
                     md:HintAssist.HelperText="{Binding NameHelper}"
                     Text="{Binding CurrentProfile.ProfileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Width="150"/>
            <Separator Style="{DynamicResource MaterialDesignSeparator}"
                       Margin="10"/>
            <ToggleButton Style="{DynamicResource MaterialDesignSwitchToggleButton}"
                          IsChecked="{Binding CurrentProfile.AddPageNumbers, Mode=TwoWay}"
                          md:HintAssist.Hint="{x:Static res:Labels.CompositionProfileDialog_PageNumbers}"/>
            <Separator Style="{DynamicResource MaterialDesignSeparator}"
                       Margin="10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button HorizontalAlignment="Center"
                        Margin="16, 0, 16, 16"
                        Command="{Binding Save}"
                        Style="{DynamicResource MaterialDesignOutlinedButton}"
                        Content="{x:Static res:Labels.CompositionProfileDialog_SaveButton}"/>
                <Button HorizontalAlignment="Center"
                        Margin="16, 0, 16, 16"
                        Command="{Binding Save}"
                        Style="{DynamicResource MaterialDesignOutlinedButton}"
                        Content="{x:Static res:Labels.CompositionProfileDialog_CancelButton}"/>
            </StackPanel>
        </StackPanel>
    </DataTemplate>

    <!--PROGRESS DIALOG-->
    <DataTemplate DataType="{x:Type dialog:ProgressDialog}">
        <StackPanel Background="{DynamicResource MaterialDesignPaper}">
            <Grid Width="40" Height="40"
                  Margin="32, 32, 32, 10">
                <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}"
                             Width="40" Height="40"
                             Value="{Binding TotalPercent}"
                             VerticalAlignment="Center" HorizontalAlignment="Center"
                             x:Name="MainProgress"/>
                <md:PackIcon x:Name="Check">
                    <md:PackIcon.Style>
                        <Style TargetType="md:PackIcon">
                            <Setter Property="Height" Value="35"/>
                            <Setter Property="Width" Value="35"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueDarkBrush}"/>
                            <Setter Property="Kind" Value="ClockOutline"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=MainProgress, Path=Value}" Value="100">
                                    <Setter Property="Kind" Value="CheckCircle"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </md:PackIcon.Style>
                </md:PackIcon>
            </Grid>
            <TextBlock Style="{DynamicResource MaterialDesignCaptionTextBlock}"
                       HorizontalAlignment="Center"
                       MinWidth="120" MaxWidth="250"
                       Margin="32, 0, 32, 5"
                       TextAlignment="Center"
                       FontSize="16"
                       Text="{Binding Phase}"/>
            <ProgressBar Style="{DynamicResource MaterialDesignLinearProgressBar}"
                         Width="120"
                         Height="10"
                         Value="{Binding PartPercent}"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Center"
                         Visibility="{Binding Part, Converter={StaticResource NullableToVisibilityConverter}}"
                         Margin="32, 0, 32, 0"/>
            <TextBlock Style="{DynamicResource MaterialDesignCaptionTextBlock}"
                       HorizontalAlignment="Center"
                       MinWidth="120" MaxWidth="250"
                       Margin="32, 0, 32, 0"
                       TextAlignment="Center"
                       FontSize="10"
                       Text="{Binding Part}"
                       Visibility="{Binding Part, Converter={StaticResource NullableToVisibilityConverter}}"/>
            <Button Command="{Binding Close}">
                <Button.Style>
                    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="16"/>
                        <Setter Property="IsEnabled" Value="False"/>
                        <Setter Property="Content" Value="{x:Static res:Labels.ProgressDialog_Cancel}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=MainProgress, Path=Value}" Value="100">
                                <Setter Property="Content" Value="{x:Static res:Labels.ProgressDialog_Close}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>