<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Style TargetType="{x:Type md:PackIcon}" x:Key="MainMenuIcon">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={RelativeSource Self}, Path=IsChecked}"
                         Value="True">
                <Setter Property="md:PackIcon.Foreground" Value="{DynamicResource SecondaryHueMidBrush}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    
</ResourceDictionary>