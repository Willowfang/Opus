<UserControl x:Class="Opus.Modules.Options.Views.MergeOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:Opus.Modules.Options.Resources"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <TextBlock HorizontalAlignment="Right" FontWeight="Bold"
                       VerticalAlignment="Center"
                       Style="{DynamicResource MaterialDesignHeadline6TextBlock}"
                       Text="{x:Static res:Labels.Merge_AddPages}"/>
            <Rectangle Width="2" Fill="LightGray" Opacity="0.5" Margin="16, 0, 16, 0" RadiusX="2" RadiusY="2"/>
            <ToggleButton Style="{DynamicResource MaterialDesignSwitchToggleButton}"
                          IsChecked="{Binding AddPages}"
                          ToolTip="Add pages"
                          VerticalAlignment="Center"
                          Cursor="Hand"/>
        </StackPanel>
    </Grid>
</UserControl>
